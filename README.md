## How to Compile and Run

To compile and run the project, follow these steps:

1. Open a terminal in the project directory.
2. Run the following command to compile the code:
   ```bash
   make
3. Then, execute the program with:
      ```bash
   ./parser tokens.txt
